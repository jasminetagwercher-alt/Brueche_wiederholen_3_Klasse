export class NumberLine{
  constructor(container,{min=0,max=1,subdivisions=4,target=0.5,mode='place',tolerance=.06}={}){Object.assign(this,{container,min,max,subdivisions,target,mode,tolerance,value:min});this.render()}
  render(){const span=this.max-this.min;let ticks='';for(let i=0;i<=this.subdivisions;i++){const p=i/this.subdivisions*100;const v=this.min+span*i/this.subdivisions;ticks+=`<span class="number-line-tick" style="left:${p}%"></span><span class="number-line-label" style="left:${p}%">${String(Number(v.toFixed(2))).replace('.',',')}</span>`}const targetP=(this.target-this.min)/span*100;this.container.innerHTML=`<div class="number-line" role="application" aria-label="Zahlenstrahl"><div class="number-line-track"></div>${ticks}${this.mode==='read'?`<span class="number-line-mark" style="left:${targetP}%"></span>`:`<span class="number-line-target" tabindex="0" style="left:0%" aria-label="Markierung verschieben"></span>`}</div>`;if(this.mode==='place')this.wire()}
  wire(){const line=this.container.querySelector('.number-line'),dot=this.container.querySelector('.number-line-target');const set=clientX=>{const r=line.getBoundingClientRect();const p=Math.max(0,Math.min(1,(clientX-r.left)/r.width));this.value=this.min+p*(this.max-this.min);dot.style.left=`${p*100}%`};line.addEventListener('pointerdown',e=>{set(e.clientX)});dot.addEventListener('keydown',e=>{const step=(this.max-this.min)/this.subdivisions;if(['ArrowLeft','ArrowDown'].includes(e.key)){this.value=Math.max(this.min,this.value-step);e.preventDefault()}if(['ArrowRight','ArrowUp'].includes(e.key)){this.value=Math.min(this.max,this.value+step);e.preventDefault()}dot.style.left=`${(this.value-this.min)/(this.max-this.min)*100}%`})}
  getAnswer(){return this.value}
  isCorrect(){return Math.abs(this.value-this.target)<=this.tolerance*(this.max-this.min)}
}
